export enum ButtonColorsEnum {
	primary = 'primary',
	secondary = 'secondary',
	inherit = 'inherit',
	default = 'default',
}

export enum ButtonTypesEnum {
	button = 'button',
	submit = 'submit',
	reset = 'reset',
}
